// export const environment = {};
export const environment = {
  production: true,
  apiBaseUrl: 'https://api.shaadrag.online', // <- your real deployed backend URL
};

// ng build --configuration production
